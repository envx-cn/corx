node_modules/
dist/
.ssr/
# wrangler's dev secrets — the example stays committable.
.dev.vars
.dev.vars.*
!.dev.vars.example
# `wrangler deploy --secrets-file` reads one of these; never commit it.
.env
.env.*
!.env.example
# Pre-rotation vars written by `npm run kek:rotate` — delete after verifying.
corx-kek-backup-*.json
# Committed, value-free examples (the real files above are ignore-listed).
!.env.production.template
.wrangler/
.wrangler/*
worker-configuration.d.ts
coverage/
